from collections import namedtuple
n = int(input())
Columns = namedtuple('Student', input().split())
total_marks = 0
for _ in range(n):
    student = Columns(*input().split())
    total_marks += int(student.MARKS)
print(f"{total_marks / n:.2f}")
